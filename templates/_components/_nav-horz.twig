<ul class="nav {{ classes | default('') }}">
    {% for link in links %}
        <li class="nav-item {% if link.url in craft.app.request.absoluteUrl %}active{% endif %}">
            <a class="nav-link"
               href="{{ link.url }}">{{ link.title }}</a>
        </li>
    {% endfor %}
</ul>

